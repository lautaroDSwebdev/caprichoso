---
import Layout from "@/layouts/Layout.astro";
import data from "../mock/data.json";
const banco = data.portfolio.portfolioPage.banco_santanderLinks;
---

<Layout title="Banco Santander">
  <div>
    <h2 class="text-white titles">Banco Santander</h2>
    <div class="flex flex-col gap-3 max-width iframe-style">
      {banco.map((e) => <iframe src={e.url} allowfullscreen />)}
    </div>
  </div>
</Layout>
